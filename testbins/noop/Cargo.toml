[workspace]
# intentionally empty, see: https://github.com/rust-lang/cargo/issues/10872#issuecomment-1188590433

[package]
name = "noop"
version = "0.1.0"
edition = "2024"

[dependencies]
acpi = { version = "5.2.0", default-features = false }
log = { version = "0.4.27", default-features = false }
uefi = { version = "0.35.0", default-features = false, features = [
  "panic_handler",
  "logger",
] }
